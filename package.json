{
  "name": "dsh-agent-compact",
  "version": "0.1.0",
  "description": "Agent-driven compaction for DeepSeek Harness: the agent summarizes its own conversation (KV-cache friendly, no giant replay requests), replacing the official replay-based compaction-basic.",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "lib"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-agent": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-compaction": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-llm": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-session": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-token-meter": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-commands": "^0.1.2-rc.1",
    "@deepseek-ai/dsh-compaction-tool-result-pruner": "^0.1.2-rc.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsdown": "^0.22.14",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noCheck",
    "typecheck": "tsc -p tsconfig.json --noEmit --noCheck"
  },
  "_comment_typeDrift": "构建用 --noCheck：本仓 plugin 的 @deepseek-ai/dsh-session 类型来自 registry（滞后，仍声明旧形态 {op,start,end}），而运行时是宿主 workspace（deepseek-harness 0.1.5-rc.1，契约为 {op,startSeq,endSeq}）。源码以宿主契约书写（权威），类型检查交给宿主仓；详见 AGENTS.md 会话事件契约条。"
}
